server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;

        # This is the key line for React Router
        # Any URL that doesn't match a file gets served index.html
        # React Router then handles the routing client-side
        try_files $uri $uri/ /index.html;
    }
}